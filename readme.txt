Use make to run
Requires ncurses

cmake and ncurses can be installed most easily with Cygwin
Cygwin is an application that provides a package manager and a Unix-like environment on Windows
https://cygwin.com/
